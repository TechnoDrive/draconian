import standard.io as io;
import standard.random as random;
import standard.collections;

io.print("> ", false);

while (io.readLine() == "flip") {
    io.print(random.from(Array.of("heads", "tails")));
    io.print("> ", false);
}